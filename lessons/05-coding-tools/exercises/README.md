# 练习

## A. 为 read 加 offset

按 [`lab.md`](../lab.md) 的实验 5 实现行偏移，先写失败测试，再改快照代码。不要直接复制 Pi 的完整实现。

## B. 区分 head 与 tail

新增 `truncate_tail()`，让 Bash 保留末尾错误，让 read/grep 保留开头。用一个 20 行字符串证明两者结果不同，同时都满足预算。

## C. 设计权限输入

当前 `Approval = Callable[[str], bool]` 只有文本。请设计一个不可变 `Operation`，至少包含工具名、相对路径或命令、是否写入。只写类型、样例与三条测试，不实现 v0.8 的完整策略。

## 验收

- 测试可离线运行；
- 失败路径不改变磁盘；
- 新字段对模型可见；
- 文档明确说明与 Pi 的差异。
